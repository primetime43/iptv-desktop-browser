<Window x:Class="DesktopApp.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="IPTV Dashboard" Height="600" Width="1000" Background="#0D1117" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border Background="#131C27" Grid.Row="0" Padding="14 0" >
            <DockPanel>
                <TextBlock Text="IPTV Dashboard" FontSize="18" FontWeight="Bold" Foreground="#F0F6FC"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <TextBlock Text="User:" Margin="16 0 4 0" Foreground="#8BA1B9"/>
                    <TextBlock x:Name="UserNameText" Foreground="#DDE6F2"/>
                    <Button Content="Logout" Margin="24 0 0 0" Click="Logout_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>
        <Grid Grid.Row="1" Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Background="#131C27" CornerRadius="10" Padding="14" Margin="0 0 12 0">
                <StackPanel>
                    <TextBlock Text="Actions" FontWeight="Bold" Foreground="#F0F6FC" Margin="0 0 8 0"/>
                    <Button Content="Load Live Categories" Margin="0 6 0 0" Click="LoadCategories_Click"/>
                    <Button Content="Load Live Streams" Margin="0 6 0 0" Click="LoadStreams_Click"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Background="#131C27" CornerRadius="10" Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Output" FontWeight="Bold" Foreground="#F0F6FC"/>
                    <TextBox x:Name="OutputText" Grid.Row="1" Margin="0 8 0 0" FontFamily="Consolas" FontSize="12" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Background="#0E1620" Foreground="#C8D3E0"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>